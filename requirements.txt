# ============================================================
#  Anime Subtitle Translator — requirements.txt
#  Install with: pip install -r requirements.txt
# ============================================================
#
#  NOTE: The following packages are NOT listed here because
#  they are large and should be installed separately:
#    pip install torch --index-url https://download.pytorch.org/whl/cpu
#    pip install transformers sentencepiece sacremoses sentence-transformers
#
#  kenlm has no official PyPI wheel for Windows.
#  Install a pre-built wheel from:
#    https://github.com/kpu/kenlm/releases
#  Or if you have Visual Studio Build Tools installed:
#    pip install https://github.com/kpu/kenlm/archive/master.zip
#
#  Ollama (for Typhoon 1.5) must be installed manually:
#    https://ollama.com  →  ollama pull scb10x/typhoon-translate1.5-4b
# ============================================================

# ── UI & window management ──────────────────────────────────
PyQt6>=6.6.0
PyOpenGL>=3.1.7

# ── Screen capture & image processing ──────────────────────
mss>=9.0.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=9.0.0

# ── OCR engine ──────────────────────────────────────────────
easyocr>=1.7.1

# ── OCR accuracy & text correction ─────────────────────────
symspellpy>=6.7.0
kenlm                        # see install note above
rapidfuzz>=3.0.0

# ── Geometry (subtitle bounding boxes) ─────────────────────
shapely>=2.0.0

# ── Translation APIs ────────────────────────────────────────
requests>=2.31.0
google-generativeai>=0.5.0
